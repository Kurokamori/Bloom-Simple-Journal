<UserControl x:Class="Bloom.Controls.SwatchPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             x:Name="Root">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="12" Margin="0,0,10,0"
                    BorderBrush="{DynamicResource BorderStrong}" BorderThickness="1.4"
                    Background="{Binding SelectedHex, ElementName=Root, Converter={StaticResource Conv.HexBrush}}" />
            <TextBox Grid.Column="1" Style="{StaticResource Field.Text}" VerticalAlignment="Center"
                     Text="{Binding SelectedHex, ElementName=Root, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
        <ItemsControl x:Name="PaletteItems" Margin="0,10,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type sys:String}">
                    <Button Width="30" Height="30" Margin="0,0,7,7" Cursor="Hand" Click="OnSwatchClick" Tag="{Binding}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="B" CornerRadius="999" BorderThickness="1"
                                        BorderBrush="{DynamicResource Border}"
                                        Background="{Binding Converter={StaticResource Conv.HexBrush}}" />
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="B" Property="BorderBrush" Value="{DynamicResource Text}" />
                                        <Setter TargetName="B" Property="BorderThickness" Value="2" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
